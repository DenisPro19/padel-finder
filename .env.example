# Set this to switch the app into public mode: it then requires the passphrase and
# listens on all interfaces. Leave it unset for local-only use.
PADEL_PASSPHRASE=choose-something-long-and-random

# Most hosts inject PORT themselves; only set it if yours does not.
# PORT=8123
